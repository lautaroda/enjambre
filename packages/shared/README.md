# shared

Esquemas (protobuf/msgpack) y tipos compartidos entre `swarm-node`, `verifier`, `ledger-daemon` y `client-sdk`.

**Pendiente.** Se puebla a medida que dos o más paquetes necesiten el mismo esquema — no antes.
